# Enable GitHub Pages

Pages is configured to deploy from **`/docs`** on branch **`main`**.

Showcase URL: `https://cobusgreyling.github.io/fleet-engineering/`

First deploy takes **1–3 minutes** after pushing to `main`. Subsequent updates follow the same window.